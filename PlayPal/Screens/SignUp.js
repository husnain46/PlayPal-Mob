import React from 'react';
import { SafeAreaView,View,TouchableOpacity, Text, ImageBackground, Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');

const SignUp = () => {

   
    return (
        <SafeAreaView style={{ flex:1}}>
            <Text>Signup</Text>
            
        
        </SafeAreaView>
    );
};

export default SignUp;
